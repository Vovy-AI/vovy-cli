---
"@vovy-ai/go": patch
"@vovy-ai/skills": patch
"@vovy-ai/mcp-server": patch
---

Sync package-level READMEs with the Context Engine / `context-scoper` / `search_codebase` features added in 0.2.0 — they still listed only the original 3 skills and `analyze_project`. npm only reads a package's own README.md, so this needs a new version to actually update what shows on npmjs.com.
